/* General Reset */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    font-family: 'Inter', sans-serif; /* A clean, modern font */
}

body {
    background-color: #121212; /* Dark background base */
    color: #ffffff;
    display: flex;
    justify-content: center;
    align-items: center;
    min-height: 100vh;
}

/* Main Container */
.container {
    display: grid;
    grid-template-columns: 1fr 1fr; /* Split screen 50/50 */
    width: 900px;
    height: 600px;
    background-color: #1e1e2e; /* Dark purple/gray background */
    border-radius: 12px;
    overflow: hidden;
    box-shadow: 0 20px 40px rgba(0, 0, 0, 0.4);
}

/* Left Section (Hero) */
.hero-section {
    background: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), 
                url('dunes-image.jpg') center/cover;
    padding: 40px;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
}

.logo {
    font-weight: bold;
    font-size: 1.5rem;
    letter-spacing: 2px;
}

.hero-content h1 {
    font-size: 2rem;
    line-height: 1.2;
    max-width: 250px;
}

/* Right Section (Form) */
.form-section {
    padding: 50px;
    display: flex;
    flex-direction: column;
    justify-content: center;
}

header h2 {
    font-size: 2rem;
    margin-bottom: 10px;
}

header p {
    color: #a0a0b0;
    font-size: 0.9rem;
    margin-bottom: 30px;
}

header a {
    color: #7c5dfa;
    text-decoration: none;
}

/* Form Controls */
.input-group {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 15px;
    margin-bottom: 15px;
}

input[type="text"],
input[type="email"],
input[type="password"] {
    width: 100%;
    padding: 12px;
    background-color: #2b2b3d;
    border: 1px solid #3f3f5a;
    border-radius: 6px;
    color: white;
    margin-bottom: 15px;
}

.btn-primary {
    width: 100%;
    padding: 14px;
    background-color: #7c5dfa; /* Purple primary color */
    border: none;
    border-radius: 6px;
    color: white;
    font-weight: bold;
    cursor: pointer;
    margin-top: 10px;
}

.btn-primary:hover {
    background-color: #6a49e3;
}

/* Social Buttons */
.divider {
    text-align: center;
    margin: 20px 0;
    font-size: 0.8rem;
    color: #666;
}

.social-auth {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 15px;
}

.btn-social {
    padding: 10px;
    background-color: transparent;
    border: 1px solid #3f3f5a;
    color: white;
    border-radius: 6px;
    cursor: pointer;
}